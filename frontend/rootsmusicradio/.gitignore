# dependencies
node_modules/

# env files (local only — .env.production is committed intentionally, no secrets)
.env
.env.local
.env.development

# builds
build/
dist/

# large media files (hosted externally — exceed Cloudflare 25MB limit)
public/logo.mp4
public/*.mp4
public/audio/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
